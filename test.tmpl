
def factorial(x: int) = if (x <= 1) 
                            1 
                        else 
                            x * factorial(x);

def main(argc: int, argv[]: str) = if (argc < 2) 
                                        print("Provide number") 
                                    else
                                        print(factorial(argv[1].to_int()));
